

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<div ng-controller="AppointmentController">
    @*@Model.Count()*@
    {{searchType}}
    <select asp-items="@ViewBag.Categories" ng-model="searchType" ng-change="CallHttpPost()"></select>
    <div class=" jumbotron " ng-repeat="x in physiotherapits">
        <div class="container">
            <div class="media">
                <img class="align-self-start mr-3" src="~/img/Profile pic/PP.png" alt="PP" height="100" width="100">
                <div class="media-body">
                    <h5 class="mt-0">Name: {{x.name}}</h5>
                    <p class="p-0">Address:{{x.address}}</p>
                    <p class="p-0">Gender: {{x.genderTypeName}}</p>
                    <p class="p-0">ContactNo: {{x.contactNo}}</p>
                    <p class="p-0">Categories: {{x.category}}</p>
                    <p class="p-0">Qualifications: {{x.qualification}}</p>

                    <a asp-controller="Home" asp-action="Physio_info" class="btn btn-primary">See profile</a>
                    @*<a asp-controller="Appointment" asp-action="TimeSlot" class="btn btn-primary">Book Appointment</a>*@
                  
                    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal" ng-click="AssignPhysiotherapist(x.physiotherapistId)">
                        Book Appointment
                    </button>

                    <br />
                </div>
            </div>
        </div>
    </div>
    @*@foreach (var item in Model)
        {
            <div class=" jumbotron ">
                <div class="container">
                    <div class="media">
                        <img class="align-self-start mr-3" src="~/img/Profile pic/PP.png" alt="PP" height="100" width="100">
                        <div class="media-body">
                            <h5 class="mt-0"> @Html.DisplayFor(modelItem => item.PhysiotherapistId)</h5>
                            <h5 class="mt-0">Dr. @Html.DisplayFor(modelItem => item.Name)</h5>

                            <p class="p-0">Specialization: @item.</p>
                            <p class="p-0">Experience:5yrs</p>
                            <p class="p-0">Consultation fee:Rs.500</p>
                            <p class="p-0">Ratings:4.5</p>
                            <a asp-controller="Appointment" asp-action="Physio_info" class="btn btn-primary">See profile</a>
                            <a asp-controller="Appointment" asp-action="Book_appointment" class="btn btn-primary">Book Appointment</a>
                            <br />
                        </div>
                    </div>
                </div>
            </div>*@
    @*<a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
        < a asp - action = "Details" asp - route - id = "@item.Id" > Details </ a > |

        < a asp - action = "Delete" asp - route - id = "@item.Id" > Delete </ a >*@

    <!-- Modal -->
    <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Available Times</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                  <table>
                      <thead>
                          <tr>
                              <th>Date</th>
                              <th>Time</th>
                              <th>Action</th>
                          </tr>
                      </thead>
                      <tbody>
                          <tr ng-repeat="x in timeSlot">
                              <td>{{x.date}}</td>
                              <td>{{x.time}}</td>
                              <td><button class="btn btn-primary" ng-click="AppointmentBook(x.physioTimeSlotsId)" >Book</button></td>
                          </tr>
                      </tbody>
                  </table>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </div>
</div>

@section scripts{ 
    <script src="~/AngularServices/Controller/AppointmentController.js"></script>
}
